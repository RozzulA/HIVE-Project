# HIVE-Project
Docker-based network management system with Pi-hole (DNS filtering), Unbound (recursive DNS), LAN Cache (game caching), and GenieACS (TR-069 ONT management). Designed for Proxmox + Ubuntu homelab environments.
